{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "msedge",
            "request": "launch",
            "name": "Launch localhost",
            "url": "http://localhost:4004",
            "webRoot": "${workspaceFolder}/",
            "runtimeArgs": ["--disable-web-security", "--user-data-dir=${workspaceFolder}/.vscode/chrome"]
        }
    ]
  }
